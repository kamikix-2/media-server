# Secretos y configuracion local - NUNCA subir al repositorio
docker/.env
docker/rclone/rclone.conf

# Datos generados por SWAG en tiempo de ejecucion (certificados, claves, logs)
docker/swag/etc/
docker/swag/keys/
docker/swag/log/
docker/swag/nginx/cache/
docker/swag/nginx/logs/
docker/swag/.donotedit
# Los proxy-confs y demas configuracion nginx SI se sincronizan con git

# Carpeta de configuracion de WireGuard (contiene claves VPN)
docker/config/

# Datos de Docker volumes montados localmente
*.log
